# Drop in /backups/.htaccess (or move backups/ outside the webroot entirely).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
